#Write a password generator in Python.The passwords should be random, generating a new password every time the user asks for a new password

import random

pass_len=random.randint(5,10)
password=random.sample("abcdefghijklmnopqrstuvwxyz01234567890ABCDEFGHIJKLMNOPQRSTUVWXYZ!@#$%^&*()", pass_len)
print("".join(password))

